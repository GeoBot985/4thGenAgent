# Optional Google Messages ABSA RPA

This folder contains an optional, private browser-backed workflow for reading ABSA debit-order SMS messages from a local Google Messages session.

It is excluded from the default portfolio path because it depends on:

- local browser state
- Google Messages authentication
- Playwright/browser availability
- personal message data

Use it only when explicitly opting in to the optional RPA tools.
