{
  "manifest_id": "gallery.side_effect_without_pending",
  "name": "Gallery: Side Effect Without Pending Expectation",
  "version": 1,
  "trigger": {"type": "manual"},
  "inputs": [],
  "steps": [
    {"id": "send_message", "command": "[t:wa/send -> result] message=\"Hello\""}
  ],
  "validations": [
    {"id": "no_errors", "type": "no_errors"}
  ],
  "completion": {
    "success_outputs": ["result"],
    "acceptable_empty_outputs": ["result"]
  },
  "live_execution": {"enabled": false, "requires_approval": true}
}
