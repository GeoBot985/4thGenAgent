{
  "manifest_id": "maintenance.report_pending_runs",
  "name": "Maintenance - Report Pending Runs",
  "version": 1,
  "trigger": {
    "type": "maintenance"
  },
  "inputs": [
    "limit",
    "rebuild"
  ],
  "steps": [
    {
      "id": "generate_pending_reports",
      "command": "[mt:report_pending_runs -> report_result] limit=$inputs.limit; rebuild=$inputs.rebuild"
    }
  ],
  "validations": [
    {
      "id": "generate_pending_reports_completed",
      "type": "step_completed",
      "step": "generate_pending_reports"
    },
    {
      "id": "report_result_exists",
      "type": "output_exists",
      "output": "report_result"
    }
  ],
  "completion": {
    "success_outputs": ["report_result"],
    "acceptable_empty_outputs": ["report_result"]
  }
}
