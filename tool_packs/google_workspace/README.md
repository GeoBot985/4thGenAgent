# Google Workspace Read-Only Tool Pack

This tool pack provides optional, read-only Gmail, Calendar, and Sheets tools.
It can be installed in a clean clone without Google credentials.

It does not send, create, update, delete, move, archive, or write anything.
Live verification is manual and only applies when credentials are configured.

Tool pack id: `google_workspace`
